import siteConfig from './siteConfig.json';

// Flip showPublications in siteConfig.json to restore the Publications nav item and /publications page.
export const SHOW_PUBLICATIONS = siteConfig.showPublications;
